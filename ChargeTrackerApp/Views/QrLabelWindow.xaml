<Window x:Class="ChargeTrackerApp.Views.QrLabelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Etichetta dispositivo" Height="420" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="DeviceNameText" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,14"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image x:Name="QrImage" Width="220" Height="220"/>
        </Border>

        <TextBlock Grid.Row="2" Text="Stampa questa etichetta e incollala sul dispositivo o sul caricatore, per riconoscerlo a colpo d'occhio."
                   Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,14,0,0" FontSize="11"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
            <Button Content="Salva immagine" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click" Margin="0,0,10,0"/>
            <Button Content="Chiudi" Style="{StaticResource SecondaryButtonStyle}" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
