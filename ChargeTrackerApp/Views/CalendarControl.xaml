<UserControl x:Class="ChargeTrackerApp.Views.CalendarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
            <Button x:Name="PrevButton" Content="◀" Width="36" Height="36" Click="PrevButton_Click" Style="{StaticResource SecondaryButtonStyle}"/>
            <TextBlock x:Name="MonthLabel" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"
                       Width="220" TextAlignment="Center" VerticalAlignment="Center" Margin="12,0"/>
            <Button x:Name="NextButton" Content="▶" Width="36" Height="36" Click="NextButton_Click" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="7" Margin="3,0,3,6">
            <TextBlock Text="Lun" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
            <TextBlock Text="Mar" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
            <TextBlock Text="Mer" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
            <TextBlock Text="Gio" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
            <TextBlock Text="Ven" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
            <TextBlock Text="Sab" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
            <TextBlock Text="Dom" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold"/>
        </UniformGrid>

        <Grid Grid.Row="2" x:Name="DaysGrid"/>
    </Grid>
</UserControl>
