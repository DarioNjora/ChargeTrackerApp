<Window x:Class="ChargeTrackerApp.Views.ExitConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chiudi ChargeTracker" Height="330" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow" ShowInTaskbar="False"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cosa vuoi fare?" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="Puoi chiudere del tutto ChargeTracker, oppure lasciarlo attivo in background: continuerà a controllare i tuoi dispositivi e ad avvisarti quando serve."
                   Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2" VerticalAlignment="Top">
            <Button Content="🔌  Rimani in background" Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Stretch" Padding="16,14" Margin="0,0,0,10" Click="Background_Click"/>
            <Button Content="✕  Chiudi definitivamente" Style="{StaticResource SecondaryButtonStyle}"
                    HorizontalAlignment="Stretch" Padding="16,14" Click="CloseCompletely_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <CheckBox x:Name="RememberCheck" VerticalContentAlignment="Top">
                <TextBlock Text="Ricorda questa scelta e non chiedermelo più (modificabile dalle Impostazioni)"
                           TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"
                           Width="330"/>
            </CheckBox>
        </StackPanel>
    </Grid>
</Window>
